<% member = potential.is_member %>
<% if member %>
  <p>
    <% if current_user.friends.include?(member.user) %>
    <!-- < %= link_to "Remove Friend", remove_friend_path(friend: member.user), :method => :delete %> -->
    You are friends
  <% else %>
    <%= button_to "Add as Friend", add_friend_path(friend: member.user), class: "add-friend" %>
  <% end %>
  </p>
<% else %>
  <%# nothing %>
<% end %>
